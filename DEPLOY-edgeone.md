# 部署指南 · 腾讯云 EdgeOne Pages 版（国内快 + 共享）

EdgeOne Pages 是「国内版的 Cloudflare Pages」：静态托管 + Edge Functions + KV 存储一体，国内节点多、访问快、长期免费，且支持直接连接 **GitHub / Gitee** 仓库自动部署。

本仓库同时保留了两套后端，互不干扰，按平台自动生效：

| 平台 | 后端目录 | 存储 |
|---|---|---|
| Cloudflare Pages | `functions/` | D1（SQL） |
| **EdgeOne Pages** | `edge-functions/` | KV 键值存储 |

> 部署到 EdgeOne 时，它只读取 `edge-functions/`，会忽略 `functions/`；反之亦然。所以同一个仓库两个平台都能部署。

---

## 一、整体流程（约 10 分钟）

1. 把代码推到 GitHub 或 Gitee
2. 在 EdgeOne Pages 创建项目，连接仓库
3. 开通 KV，创建命名空间，绑定到项目（变量名 `WC_KV`）
4. 设置环境变量 `ADMIN_SECRET`（管理员口令）
5. 重新部署，拿到默认域名发给好友

---

## 二、详细步骤

### 第 0 步：推送代码

```bash
git add -A
git commit -m "feat: 新增 EdgeOne Pages 后端（Edge Functions + KV）"
git push
```

### 第 1 步：创建 EdgeOne Pages 项目

1. 进入 [EdgeOne Pages 控制台](https://pages.edgeone.ai/)（腾讯云账号登录）
2. **创建项目 → 导入 Git 仓库**，授权并选择本仓库（GitHub 或 Gitee 都行）
3. 构建配置（纯静态、无需构建）：
   - **构建命令**：留空
   - **输出目录**：填 `/`（或留空，即仓库根目录）
   - **加速区域**：想要国内快就选「**中国大陆**」或「**全球**」区域
4. 点击 **开始部署**（先部署一次，生成项目）

> 说明：选「中国大陆/全球」区域时，**绑定自己的域名才需要 ICP 备案**；用平台分配的默认域名（形如 `xxx.edgeone.app`）**无需备案**，可直接用。

### 第 2 步：开通 KV 并创建命名空间

1. 控制台左侧 **存储 → KV**
2. 点 **立即申请** 开通 KV 账户（免费版 1GB）
3. 点 **创建命名空间**，名字填 `worldcup2026`，创建

### 第 3 步：把命名空间绑定到项目（关键）

1. 进入你的 Pages 项目 → **KV 存储** 菜单 → **绑定命名空间**
2. 选择刚创建的 `worldcup2026`
3. **运行时变量名**：必须填 `WC_KV`（大写，代码里就是用这个全局变量）
4. 保存

> ⚠️ 变量名一定要是 `WC_KV`，否则后端找不到数据库会报「KV 未绑定」。

### 第 4 步：设置管理员口令

1. 项目 → **设置 / 环境变量**
2. 新增一条：
   - **变量名**：`ADMIN_SECRET`
   - **值**：自己设一个口令，比如 `wc2026admin`（录入真实比分时要用）
3. 保存

### 第 5 步：重新部署生效

绑定 KV 和环境变量后，需要**重新部署一次**才生效：
- 项目 → **部署** → 触发一次新部署（或再 `git push` 一次）

完成后访问项目的默认域名，把链接发到微信即可。

---

## 三、怎么用

- **好友**：打开网址 → 注册账号 → 关注球队、竞猜，自动进入共享积分榜
- **你（管理员）**：比赛结束后进「我的 → 🛠️ 赛果录入」，第一次保存比分时输入 `ADMIN_SECRET` 的值（本机会记住）；录入真实比分后所有人的积分自动结算
- **积分规则**：仅在录入真实比分后结算（模拟比分不计分）
- 5 个演示玩家是前端虚拟的，纯热闹用

---

## 四、KV 存储的两个注意点

1. **最终一致、最长 60 秒**：某人下注后，其他人在别的边缘节点看积分榜，最长可能有 60 秒延迟才同步（自己的操作在本节点立即可见）。朋友竞猜场景完全够用。
2. **数据结构**（KV key 只允许数字/字母/下划线，故用户名等做了十六进制编码）：
   - `results` → 所有真实比分
   - `user_<hex 用户名>` → 账号信息（加盐哈希密码、关注列表）
   - `bets_<hex 用户名>` → 该用户的竞猜
   - `session_<hex token>` → 登录会话

---

## 五、比分自动更新（EdgeOne 版）

仓库内 `edge-functions/cron-update.js` 是 EdgeOne 版的比分自动更新，部署后自动映射为路由 **`/cron-update`**。它找出「已结束但还没比分」的小组赛，从数据源抓比分写入 KV 的 `results`，前端积分榜随之结算。

**1. 配置环境变量（项目 → 环境变量）**

| 变量名 | 说明 |
|---|---|
| `CRON_KEY` | 调用口令（未设则回退用 `ADMIN_SECRET`） |
| `SOURCE` | `footballdata`（推荐）或 `baidu`；不填则有 token 用 footballdata 否则 baidu |
| `FOOTBALL_DATA_TOKEN` | 用 footballdata 源时必填，[免费注册](https://www.football-data.org/client/register) |

**2. 手动触发 / 验证**

浏览器或 curl 访问（会立即跑一次并返回明细 JSON）：

```
https://你的域名/cron-update?key=你的CRON_KEY
```

**3. 每 4 小时自动跑（二选一）**

- **EdgeOne Pages「定时任务」**：若你的项目控制台有定时任务/Cron 功能，新建一个每 4 小时定时 **GET** `/cron-update?key=...` 即可。
- **外部定时器（通用兜底）**：用免费的 [cron-job.org](https://cron-job.org/) 或腾讯云 SCF 定时触发器，每 4 小时请求上面的 URL。

> 只处理小组赛 72 场（对阵固定）；不覆盖已有比分（含手动录入）；抓不到的下次重试，不写错误数据。淘汰赛对阵需出线后确定，仍走手动录入。

---

## 六、和 Cloudflare 版的关系

- 两套后端共存于同一仓库，**前端代码完全一样**，都通过 `/api/*` 调用，无需为某个平台改前端。
- 想用哪个平台就在哪个平台建项目连这个仓库即可；也可以两个都部署（两份独立数据）。
- 密码以「加盐 SHA-256 哈希」存储，不存明文；这是娱乐应用，**请勿使用重要密码**。
